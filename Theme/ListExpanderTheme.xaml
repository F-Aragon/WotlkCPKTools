<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Expanders -->
    <Style x:Key="ListExpanderStyle" TargetType="Expander">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="IsExpanded" Value="True"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Expander">
                    <Border Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <!-- Header -->
                                <RowDefinition Height="*"/>
                                <!-- Content -->
                            </Grid.RowDefinitions>

                            <!-- Header -->
                            <Grid Grid.Row="0" VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <!-- Arrow -->
                                    <ColumnDefinition Width="*"/>
                                    <!-- Header ContentPresenter -->
                                    <ColumnDefinition Width="Auto"/>
                                    <!-- Buttons -->
                                </Grid.ColumnDefinitions>

                                <!-- ToggleButton -->
                                <ToggleButton x:Name="ExpanderToggle"
                                              IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                              Width="20" Height="20" Margin="2"
                                              Background="Transparent" BorderThickness="0"
                                              Grid.Column="0">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="BorderBrush" Value="Transparent"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ToggleButton.Style>
                                    <TextBlock x:Name="ArrowIcon" Text="▼" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                </ToggleButton>

                                <!-- Header ContentPresenter -->
                                <Grid Grid.Column="1" VerticalAlignment="Center">
                                    <ContentPresenter ContentSource="Header" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                </Grid>

                                <!-- Right buttons -->
                                <StackPanel x:Name="RightButtonsPanel"
                                            Orientation="Horizontal"
                                            Grid.Column="2"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Center">
                                    <!-- Extra buttons if needed -->
                                </StackPanel>
                            </Grid>

                            <!-- Expander content -->
                            <ContentPresenter x:Name="PART_Content" Grid.Row="1" Visibility="Collapsed" Margin="{TemplateBinding Padding}"/>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <!-- Show content if IsExpanded = True -->
                        <Trigger Property="IsExpanded" Value="True">
                            <Setter TargetName="PART_Content" Property="Visibility" Value="Visible"/>
                        </Trigger>

                        <!-- Change icon depending on IsChecked -->
                        <Trigger SourceName="ExpanderToggle" Property="IsChecked" Value="True">
                            <Setter TargetName="ArrowIcon" Property="Text" Value="▲"/>
                        </Trigger>
                        <Trigger SourceName="ExpanderToggle" Property="IsChecked" Value="False">
                            <Setter TargetName="ArrowIcon" Property="Text" Value="▼"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
