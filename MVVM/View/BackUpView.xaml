<UserControl x:Class="WotlkCPKTools.MVVM.View.BackUpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WotlkCPKTools.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10">
            <!-- Button to start full WTF backup -->
            <Button Content="Full WTF"
                    Height="26"
                    Command="{Binding BackupWtfCommand}"/>

            <!-- TextBlock to show current backup path -->
            <TextBlock Margin="10,0,0,0"
                       Width="400"
                       Text="{Binding CurrentBackupPath}"
                       Background="DarkGray"
                       Foreground="White"
                       TextTrimming="CharacterEllipsis"/>

            <!-- TextBlock to show backup status -->
            <TextBlock Margin="10,0,0,0"
                       Width="100"
                       Text="{Binding BackupStatus}"
                       Foreground="White"/>
        </StackPanel>

    </Grid>
</UserControl>
