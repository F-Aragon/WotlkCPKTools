<Window x:Class="WotlkCPKTools.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WotlkCPKTools"
        xmlns:viewModel="clr-namespace:WotlkCPKTools.MVVM.ViewModel"
        mc:Ignorable="d"
        Height="600" Width="800"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True">

    <Window.DataContext>
        <viewModel:MainViewModel/>
    </Window.DataContext>

    <Border Background="Black" CornerRadius="10" MouseDown="_MouseDown">
        <Grid>
            <Grid Margin="5" Background="#272537">
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition/>
                    <RowDefinition Height="30"/>
                </Grid.RowDefinitions>

                <!-- Header Row -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Title -->
                    <TextBlock Text="CPK WoW Tools"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"
                               Foreground="White"
                               FontSize="22"/>

                    <!-- Menu -->
                    <StackPanel Orientation="Horizontal" Grid.Column="1" >
                        <RadioButton Content="Addons"
                                     Width="140"
                                     Foreground="White"
                                     FontSize="30"
                                     Style="{StaticResource MenuButtonTheme}"
                                     IsChecked="True"
                                     Command="{Binding AddonsViewCommand}"/>

                        <RadioButton Content="Backup"
                                     Width="140"
                                     Foreground="White"
                                     FontSize="30"
                                     Style="{StaticResource MenuButtonTheme}"
                                     Command="{Binding BackUpViewCommand}"/>

                        <RadioButton Content="Extra"
                                     Width="140"
                                     Foreground="White"
                                     FontSize="30"
                                     Style="{StaticResource MenuButtonTheme}"
                                     Command="{Binding ExtrasViewCommand}"/>
                    </StackPanel>

                    <!-- Minimize & Close Buttons -->
                    <StackPanel Orientation="Horizontal" Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Top">
                        <Button Width="25" Height="20" Margin="2"
                                Content="_"
                                FontWeight="Bold"
                                Foreground="White"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                Click="MinimizeWindow_Click"/>

                        <Button Width="25" Height="20" Margin="2"
                                Content="X"
                                FontWeight="Bold"
                                Foreground="White"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                Click="CloseWindow_Click"/>
                    </StackPanel>
                </Grid>

                <!-- Content -->
                <ContentControl Grid.Row="1"
                                Margin="10,10,10,5"
                                Content="{Binding CurrentView}"/>

                <!-- Footer / Optional controls -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
